window.SardineFish=(function(sar){try{    if(!sar)        sar=function(){}    sar.Web=(function(web){if(!web)web=function(){};return web;})(sar.Web);    function LinkList()    {        this.head=null;        this.tail=null;        this.count=0;    }    LinkList.Node=function(obj, last, next)    {        this.object=obj;        if(last)            this.last=last;        else            this.last=null;        if(next)            this.next=next;        else            this.next=null;    }    LinkList.prototype.add=function(obj)    {        if(this.count<=0)        {            this.head=new LinkList.Node(obj,null,null);            this.tail=this.head;            this.count=1;            return 1;        }        this.tail.next=new LinkList.Node(obj,this.tail,null);        this.tail=this.tail.next;        this.count++;        return this.count;    }    sar.Web.LinkList=LinkList;    window.LinkList=LinkList;        return sar;}catch(ex){alert(ex.message);}})(window.SardineFish);