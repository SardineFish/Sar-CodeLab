{
  "name": "rain",
  "version": "1.0.0",
  "description": "Rain drop effect with WebGL",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.js --bundle --minify --outfile=dist/index.js",
    "build:watch": "npx esbuild ./src/index.js --bundle --outfile=dist/index.js --watch",
    "check": "npx tsc",
    "check:watch": "npx tsc -w"
  },
  "author": "SardineFish",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.8.50",
    "typescript": "^4.1.5"
  }
}
